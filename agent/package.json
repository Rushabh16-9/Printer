{
  "name": "printer-by-scanner-agent",
  "version": "2.0.0",
  "description": "PrintScan print agent for Windows — auto-detects printer, receives jobs from cloud server",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js",
    "dev": "nodemon agent.js"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "pdf-to-printer": "^5.3.1",
    "socket.io-client": "^4.7.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}

